<%= form_with(model: newsletter, class: 'col-md-6') do |form| %>
  <% if newsletter.errors.any? %>
    <div style="color: red">
      <h5><%= pluralize(newsletter.errors.count, "error") %> prohibited this newsletter from being saved:</h5>

      <ul>
        <% newsletter.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :body, style: "display: block" %>
    <%= form.text_field :body, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :publish_at, style: "display: block" %>
    <%= form.datetime_field :publish_at, class: 'form-control', step: 1 %>
  </div>

  <div class="form-group mt-2">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
